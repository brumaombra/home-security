<script setup>
// Redirect to events page
definePageMeta({
    redirect: '/events'
});

// Page metadata
useHead({
    title: 'Home Security'
});
</script>

<template>
    <div>
        <!-- Redirecting to events page... -->
    </div>
</template>